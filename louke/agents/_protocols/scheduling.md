# Agent Scheduling Protocol (louke scheduling protocol)

## 1. Purpose

Defines the rules for louke multi-agent **serial collaboration on the same `releases/{version}` branch**. Maestro is the sole scheduling arbiter; all subagents must obey Maestro's arrangement and must not decide on their own when to write code, when to review, or when to fix.

**Applicable scenarios**:
- Maestro arranges the serial order of code-writing agents such as Devon/Prism/Keeper/Shield
- Any agent reports a potential concurrency conflict when detected
- Design the queuing rules for write operations such as review/gate fixes

## 2. Core Rules

### 2.1 Single Active Branch

- Each version has only one active development branch: `releases/{version}`
- Creating task-level branches (`feat/...`, `fix/...`, `task/...`) is forbidden
- Feature development outside `releases/{version}` is forbidden

### 2.2 Single Writer

- At any given moment, **only one agent is allowed to produce commits/pushes** on `releases/{version}`
- The writer is explicitly designated by Maestro, typically a Devon instance handling the R-G-R cycle of a single issue
- Other agents that need to write (Prism/Keeper/Shield, etc.) must be scheduled by Maestro and can take over only after the current writer finishes and pushes

### 2.3 Read-Operation Window

- **Read-only reviews** during the current writer's work (Prism review, Archer look-back, Judge check) should also happen under Maestro's scheduling
- Ideally, read-only reviews begin after the current writer has pushed all three R-G-R phases, to avoid reading intermediate inconsistent states

### 2.4 Task Isolation

- Tasks are isolated by **commit**, not by branch
- Each R-G-R phase gets one independent commit
- The number of the issue currently being handled should appear in the commit message (auto-generated by `commit-rgr`)

## 3. Violation Handling

When any agent detects the following signs, **immediately stop the current work and report to Maestro**:

- `git log` contains interleaved commits not produced by the current task
- CI shows multiple PRs/workflows targeting `releases/{version}` at the same time
- Unexpected modifications appear in the current working directory
- Another agent produced a commit/push on the same branch

Maestro decides which side to keep, rolling back to the last consistent state if necessary.

## 4. Minimum Responsibilities of Each Agent

| Agent | Responsibility |
|---|---|
| **Maestro** | Sole scheduling arbiter; decides who writes, who reads, and who waits |
| **Devon** | Does not create branches; handles one issue at a time; pushes immediately after committing; reports concurrency conflicts immediately when detected |
| **Prism** | Does not write fixes to the current branch unless explicitly scheduled by Maestro; reviews are read-only |
| **Keeper** | Gate fixes must be scheduled by Maestro; no queue jumping |
| **Shield** | e2e test write operations must be scheduled by Maestro |

## 5. Version

- v1.0 — 2026-07-05 — Extracted from Maestro.md's concurrency constraints as an independent protocol
