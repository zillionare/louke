# v0.7 Release TODO

记录于 2026-07-07 00:15 (本会话末)

## 4 件事

### 1. inline-comments 补全

`louke/agents/_skills/inline-comments/SKILL.md` 补 "How to read" 段：

- 找到"会话断点"（当前需要我回复的 quote）
- 3 类别（per speaker）：
  - `unanswered` — 我起的对话，**无回复**
  - `unresolved` — 我起的对话，有回复但**未置 resolved**
  - `awaiting_my_reply` — **@提及我**的 quote，**我未回**
- 会话断点概念：每条 quote 是对话链节点，找"last open quote" 继续
- **禁止** skill 中出现 `python -m ...` —— 只用 `lk`（保证 venv/依赖）

**前置依赖**：`quote_parser.py` 需加 `--for <speaker>` flag（当前**没有**），把 filter 后的 3 类别输出到 JSON/text。

### 2. Sage / Prism review

**Sage** 重点改：
- **强调** `permission.question: allow`（`quote` skill 不替代 question tool）
- 强调**邀请用户 review spec**（Step 1 → Step 2 的过渡：发问 → 等用户回）
- 强调"沉默即同意"是禁止的（FR-0030 AC-3 已写）
- **不要**用 `question` tool 写 quote dialogue —— question 是 UI dialog，quote 是文档交互

**Prism** 重点改：
- 评审意见用 quote dialogue 形式写入
- 拒绝 → 退回实施者修订，**不**自己改

### 3. 完整运行 v0.7

- `lk init` 跑通（无已存在项目模式）
- 走完 M-FOUND → M-MILESTONE 11 阶段
- 验证每阶段 holdpoint：`lk agent sage quote-check` / `lk keeper gate` / `lk shield run-e2e` / `lk judge security-audit` 等
- 跑 v0.7-002 (knowledge-distillation-karpathy) 实际场景，验证 spec / acceptance / quote dialogue 链路
- 记录任何卡点

### 4. 发版本，本机更新 `~/workspace/millionaire`

- `git tag v0.7.0` + 推到远端
- `pip install --upgrade louke` 在 millionaire 项目
- 验证 louke 装上后 millionaire 仍能跑现有 louke 命令
- 写 `.github/RELEASE_NOTES_v0.7.0.md` 列出 v0.7 的 5+ 项改动：
  1. v0.7-001 pre-commit 接管 lint/format/typecheck/test
  2. v0.7-002 knowledge distillation Karpathy 化
  3. fix-000 → v0.6-016 quote dialogue 协议正式化
  4. fix-001a e2e env contract via project-config.toml
  5. fix-002 project.toml 替换 project-info.md
  6. keeper slim（移除 lint/typecheck/test 路径，pre-commit 接管）
