#!/usr/bin/env sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
PYTHON="$ROOT/.venv/bin/python"
if [ ! -x "$PYTHON" ]; then
  echo "$PYTHON: project-venv Python is missing or not executable" >&2
  exit 127
fi

exec "$PYTHON" "$ROOT/tests/e2e/run_e2e.py" "$@"
