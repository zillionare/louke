{
  "kind": "secret-canaries",
  "identity": "fixture.matrix.secret-canaries.v014-002-shield",
  "revision": "shield-fixture-v1",
  "note": "All canaries are synthetic, unique, and injected only via external fixtures. No real credentials. Any report must NOT echo these values.",
  "canaries": [
    {
      "id": "github-token",
      "value": "ghp_SHIELDFIXTURE0000000000000000000000CANARY",
      "kind": "github_pat",
      "expected": "scanner blocks baseline; location reported; value NOT echoed"
    },
    {
      "id": "pypi-token",
      "value": "pypi-AGAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAASHIELDFIXTURE",
      "kind": "pypi_upload",
      "expected": "scanner blocks baseline; location reported; value NOT echoed"
    },
    {
      "id": "cookie",
      "value": "session=SHIELDFIXTURECANARY0000000000000000000000",
      "kind": "session_cookie",
      "expected": "scanner blocks baseline; location reported; value NOT echoed"
    },
    {
      "id": "api-key",
      "value": "sk-shield-fixture-canary-0000000000000000000000",
      "kind": "api_key",
      "expected": "scanner blocks baseline; location reported; value NOT echoed"
    }
  ]
}
